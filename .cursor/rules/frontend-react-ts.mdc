---
description: React + TypeScript 开发约定（仅前端源码生效）
globs: web/packages/web/src/**/*.{ts,tsx}
alwaysApply: false
---

# 前端（React/TS）规则

## 目录与导入

- 页面优先放在 `web/packages/web/src/pages/**`，通用组件放在 `web/packages/web/src/components/**`。
- 导入顺序建议：第三方库 → 内部别名 `@/` → 相对路径；避免循环依赖。

## 组件与状态

- 组件默认使用函数组件与 hooks。
- 状态管理保持一致：局部状态用 React state/hooks；跨页面/全局状态按项目现有选择（如已使用的 store 方案）接入，不要在同一模块混用多套方案。
- effect/订阅类逻辑必须有清理；异步请求注意竞态（请求取消/忽略过期响应）。
- 禁止在 render 过程中直接发起请求/写入存储/触发副作用；副作用仅放在事件处理或 effect 中。
- 表单提交、按钮点击等异步操作必须具备防重复触发控制（如 `loading` 锁或 disabled）。

## 路由与页面

- 新增页面时同步更新路由与导航入口（如果项目有集中路由表/菜单）。
- 页面标题、loading、empty/error 状态要可用且一致（沿用项目已有组件/模式）。
- page 页面目录建议按业务分层：`pages/<module>/<page>/`，页面入口命名为 `index.tsx`。
- 页面内文件职责建议拆分：`index.tsx`（页面装配）/ `components/*`（页面私有组件）/ `hooks/*`（页面逻辑）/ `types.ts`（页面模型类型）。
- 列表页默认包含：查询区、表格区、分页区；查询条件变更时重置到第一页。
- 页面请求必须覆盖请求中与失败态：避免重复提交，错误提示使用项目统一消息组件。
- 页面状态持久化遵循项目现有模式：已支持 URL 查询参数同步的页面，筛选和分页状态应可回填。
- 新页面组件命名使用 `PascalCase`，建议以 `Page` 结尾（如 `UserListPage`）。
- 单个 page 入口文件（`index.tsx`）建议不超过 300 行；超过时必须拆分为子组件或 hooks。
- 页面内新增类型优先写在 `types.ts` 或相邻 `*.types.ts`，避免在业务逻辑中内联超长类型声明。

## 性能与可维护性

- 避免在 render 中创建不必要的重对象/匿名函数导致子组件重复渲染；必要时使用 `useMemo/useCallback`。
- 大组件优先拆分：视图与逻辑分离（hook + view），复杂计算移到工具函数。
- 列表页的 `rowKey` 必须稳定且可追踪；禁止使用随机值或会变化的索引作为长期 key。
- 新增代码默认补齐基础错误边界处理：至少保证请求失败后界面可继续操作。

## 禁止项（强约束）

- 禁止在同一页面混用两套 UI 方案（例如同类表单控件同时使用不同设计体系）。
- 禁止无理由新增 `any`、`as any`、`@ts-ignore`；确需使用时必须在同文件给出简短理由注释。
- 禁止在页面开发任务中顺手修改无关模块（样式大改、公共组件重构、目录迁移）。

